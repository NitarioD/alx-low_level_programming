#include "main.h"

/**
 * main - check the code
 *
 * Description: function to print absolute value of a number
 *
 * Return: Always 0.
 */
int _abs(int no)
{
	if (no < 0)
	{
		no *= -1;
	}
	return (no);
}
