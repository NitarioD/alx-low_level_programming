#include "main.h"

/**
 * main - check the code
 *
 * Description: function that returns the addition of two integers
 *
 * Return: Always 0.
 */
int add(int first, int second)
{
	return (first + second);
}
